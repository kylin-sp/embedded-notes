# 发布指南：把笔记网页部署到线上

本页面是单文件静态网页（`index.html`），任何静态托管平台都能直接运行，无需服务器。下面提供 3 个免费方案，任选其一，约 10 分钟完成。

## 方案 A：GitHub Pages（推荐，长期免费、带版本管理）
需要：一个 GitHub 账号（没有就在 github.com 免费注册）。

1. 登录 github.com → 右上角 `+` → `New repository`
2. 仓库名填 `embedded-notes`，可见性选 Public，然后 `Create repository`
3. 上传本目录的 `index.html` 和 `README.md`：
   - 网页方式：仓库页 → `Add file` → `Upload files` → 把两个文件拖进去 → `Commit changes`
   - 或命令行（本机已装 git 2.53）：
     ```bash
     cd C:\Users\15219\Documents\Codex\2026-08-03\b-n\outputs\embedded-notes
     git init
     git add .
     git commit -m "初始版本"
     git branch -M main
     git remote add origin https://github.com/<你的用户名>/embedded-notes.git
     git push -u origin main
     ```
4. 开启 Pages：仓库页 → `Settings` → 左侧 `Pages` → Source 选 `Deploy from a branch` → 分支选 `main`，目录选 `/(root)` → `Save`
5. 等 1–2 分钟，访问 `https://<你的用户名>.github.io/embedded-notes/`

## 方案 B：Vercel（最省事，网页拖拽上传，不需要 git）
需要：一个 Vercel 账号（vercel.com，支持 GitHub / Google 登录）。

1. 登录 vercel.com → Dashboard → `New Project`
2. 选 `Upload`（拖拽上传文件夹，无需 Git 仓库）
3. 把 `outputs\embedded-notes` 整个文件夹拖进去 → `Deploy`
4. 完成后得到 `https://xxx.vercel.app` 链接，直接可用

## 方案 C：Gitee Pages（国内访问最快，但需实名认证 + 人工审核）
需要：一个 Gitee 账号并完成实名认证。

1. gitee.com 注册并实名认证
2. 新建仓库，上传 `index.html`
3. 仓库页 → `服务` → `Gitee Pages` → 部署（每次更新都要重新提交审核）

## 注意事项
- 笔记数据存在浏览器 localStorage：线上版只在「当前电脑的当前浏览器」里读写，换浏览器、换设备看不到之前的笔记。这是下一步「自动云同步」要解决的问题。
- 页面内置 5 条示例记录（问题 / 知识点 / 经验），第一次打开即可看到 Markdown 效果。
